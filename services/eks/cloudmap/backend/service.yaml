
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: backend-namespace
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: backend-app
  ports:
    - port: 80
      targetPort: 80
